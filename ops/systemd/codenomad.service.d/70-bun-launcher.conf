[Service]
# Keep the installed package and all base-unit arguments unchanged; only replace
# the Node CLI shim with Bun running the compiled server entrypoint directly.
ExecStart=
ExecStart=/home/andual/.bun/bin/bun /home/andual/.nvm/versions/node/v22.23.2/lib/node_modules/@neuralnomads/codenomad/dist/index.js --host 127.0.0.1 --http true --https false --http-port 4400 --workspace-root /home/andual --password omp
